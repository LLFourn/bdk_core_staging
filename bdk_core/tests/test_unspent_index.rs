// use bdk_core::{ChangeSet, SparseChain, SpkTracker, UnspentIndex, Update, testing::{gen_block_id, gen_hash}, Change, TxHeight};

#[test]
fn single_spends() {
    // let mut chain = SparseChain::default();
    // let mut tracker = SpkTracker::<()>::default();
    // let mut index = UnspentIndex::<()>::default();

    // assert_eq!(
    //     chain.apply_update(Update {
    //         txids: [(gen_hash(1000), TxHeight::Confirmed(1))].into(),
    //         ..Update::new(None, gen_block_id(1, 1))
    //     }),
    //     Ok(ChangeSet {
    //         ..Default::default()
    //     })
    // );
}
